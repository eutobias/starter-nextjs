import { RegisterForm } from "@/features/auth/register/register.component";

export default function RegisterPage() {
  return (
    <div className="min-h-screen bg-gray-50 flex items-center justify-center">
      <RegisterForm />
    </div>
  )
}